[package]
name = "rust-project"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
godot = "0.5.3"
derive_more = { version = "2.1", features = ["add", "add_assign", "display", "from", "into", "mul"] }
